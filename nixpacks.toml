[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gnumake", "gcc"]
onlyIncludeFiles = []

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/server.js"
